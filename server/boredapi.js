const mongoose = require ('mongoose');
const Schema = mongoose.Schema;

const bored {
  activity: String
  accessibility: Float
  type: String
  participants: Int,
  price: Float,
  link: String,
  key: Int
}
